pipeline {
    agent any
    
    tools {
        maven "MAVEN_HOME"
        jdk "JAVA_HOME"
    }
    environment {
        SNAP-REPO = 'vprofile-snapshot'
        NEXUS-USER = 'admin'
        NEXUS-PASS = 'ZAQ!zaq1'
        RELEASE-REPO = 'vprofile-release'
        CENTRAL-REPO = 'vpro-maven-central'
        NEXUSIP = '10.0.2.179'
        NEXUSPORT = '8081'
        NEXUS-GRP-REPO = 'vprofile-maven-group'
        NEXUSLOGIN = 'Nexus Cred'
    }

    stages {
        stage('Build') {
            steps {
                sh 'mvn -s settings.xml -DskipTests install'
            }
        }
    }
}
